import { PluralisePipePipe } from './pluralise-pipe.pipe';

describe('PluralisePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PluralisePipePipe();
    expect(pipe).toBeTruthy();
  });
});
